CREATE TABLE IF NOT EXISTS "__EFMigrationsHistory" (
    "MigrationId" character varying(150) NOT NULL,
    "ProductVersion" character varying(32) NOT NULL,
    CONSTRAINT "PK___EFMigrationsHistory" PRIMARY KEY ("MigrationId")
);

START TRANSACTION;

DO $EF$
BEGIN
    IF NOT EXISTS(SELECT 1 FROM pg_namespace WHERE nspname = 'auth') THEN
        CREATE SCHEMA auth;
    END IF;
END $EF$;

CREATE TABLE auth.roles (
    "Id" uuid NOT NULL,
    "Name" character varying(256),
    "NormalizedName" character varying(256),
    "ConcurrencyStamp" text,
    CONSTRAINT "PK_roles" PRIMARY KEY ("Id")
);

CREATE TABLE auth.users (
    "Id" uuid NOT NULL,
    "DisplayName" text NOT NULL,
    "AvatarUrl" text,
    "IsActive" boolean NOT NULL,
    "CreatedAt" timestamp with time zone NOT NULL,
    "UpdatedAt" timestamp with time zone NOT NULL,
    "LastLoginAt" timestamp with time zone,
    "DeletedAt" timestamp with time zone,
    "UserName" character varying(256),
    "NormalizedUserName" character varying(256),
    "Email" character varying(256),
    "NormalizedEmail" character varying(256),
    "EmailConfirmed" boolean NOT NULL,
    "PasswordHash" text,
    "SecurityStamp" text,
    "ConcurrencyStamp" text,
    "PhoneNumber" text,
    "PhoneNumberConfirmed" boolean NOT NULL,
    "TwoFactorEnabled" boolean NOT NULL,
    "LockoutEnd" timestamp with time zone,
    "LockoutEnabled" boolean NOT NULL,
    "AccessFailedCount" integer NOT NULL,
    CONSTRAINT "PK_users" PRIMARY KEY ("Id")
);

CREATE TABLE auth.role_claims (
    "Id" integer GENERATED BY DEFAULT AS IDENTITY,
    "RoleId" uuid NOT NULL,
    "ClaimType" text,
    "ClaimValue" text,
    CONSTRAINT "PK_role_claims" PRIMARY KEY ("Id"),
    CONSTRAINT "FK_role_claims_roles_RoleId" FOREIGN KEY ("RoleId") REFERENCES auth.roles ("Id") ON DELETE CASCADE
);

CREATE TABLE auth.identity_user_tokens (
    "UserId" uuid NOT NULL,
    "LoginProvider" text NOT NULL,
    "Name" text NOT NULL,
    "Value" text,
    CONSTRAINT "PK_identity_user_tokens" PRIMARY KEY ("UserId", "LoginProvider", "Name"),
    CONSTRAINT "FK_identity_user_tokens_users_UserId" FOREIGN KEY ("UserId") REFERENCES auth.users ("Id") ON DELETE CASCADE
);

CREATE TABLE auth.refresh_tokens (
    "Id" uuid NOT NULL,
    "UserId" uuid NOT NULL,
    "FamilyId" uuid NOT NULL,
    "TokenHash" text NOT NULL,
    "ExpiresAt" timestamp with time zone NOT NULL,
    "CreatedAt" timestamp with time zone NOT NULL,
    "UsedAt" timestamp with time zone,
    "RevokedAt" timestamp with time zone,
    "ReplacedByTokenId" uuid,
    "IsRevoked" boolean NOT NULL,
    "Reason" text,
    "CreatedByIp" text,
    "Device" text,
    CONSTRAINT "PK_refresh_tokens" PRIMARY KEY ("Id"),
    CONSTRAINT "FK_refresh_tokens_refresh_tokens_ReplacedByTokenId" FOREIGN KEY ("ReplacedByTokenId") REFERENCES auth.refresh_tokens ("Id") ON DELETE RESTRICT,
    CONSTRAINT "FK_refresh_tokens_users_UserId" FOREIGN KEY ("UserId") REFERENCES auth.users ("Id") ON DELETE CASCADE
);

CREATE TABLE auth.user_claims (
    "Id" integer GENERATED BY DEFAULT AS IDENTITY,
    "UserId" uuid NOT NULL,
    "ClaimType" text,
    "ClaimValue" text,
    CONSTRAINT "PK_user_claims" PRIMARY KEY ("Id"),
    CONSTRAINT "FK_user_claims_users_UserId" FOREIGN KEY ("UserId") REFERENCES auth.users ("Id") ON DELETE CASCADE
);

CREATE TABLE auth.user_logins (
    "LoginProvider" text NOT NULL,
    "ProviderKey" text NOT NULL,
    "ProviderDisplayName" text,
    "UserId" uuid NOT NULL,
    CONSTRAINT "PK_user_logins" PRIMARY KEY ("LoginProvider", "ProviderKey"),
    CONSTRAINT "FK_user_logins_users_UserId" FOREIGN KEY ("UserId") REFERENCES auth.users ("Id") ON DELETE CASCADE
);

CREATE TABLE auth.user_roles (
    "UserId" uuid NOT NULL,
    "RoleId" uuid NOT NULL,
    CONSTRAINT "PK_user_roles" PRIMARY KEY ("UserId", "RoleId"),
    CONSTRAINT "FK_user_roles_roles_RoleId" FOREIGN KEY ("RoleId") REFERENCES auth.roles ("Id") ON DELETE CASCADE,
    CONSTRAINT "FK_user_roles_users_UserId" FOREIGN KEY ("UserId") REFERENCES auth.users ("Id") ON DELETE CASCADE
);

CREATE TABLE auth.user_tokens (
    "Id" uuid NOT NULL,
    "UserId" uuid NOT NULL,
    "Type" integer NOT NULL,
    "TokenHash" text NOT NULL,
    "ExpiresAt" timestamp with time zone NOT NULL,
    "CreatedAt" timestamp with time zone NOT NULL,
    "UsedAt" timestamp with time zone,
    CONSTRAINT "PK_user_tokens" PRIMARY KEY ("Id"),
    CONSTRAINT "FK_user_tokens_users_UserId" FOREIGN KEY ("UserId") REFERENCES auth.users ("Id") ON DELETE CASCADE
);

CREATE INDEX "IX_refresh_tokens_FamilyId" ON auth.refresh_tokens ("FamilyId");

CREATE INDEX "IX_refresh_tokens_ReplacedByTokenId" ON auth.refresh_tokens ("ReplacedByTokenId");

CREATE UNIQUE INDEX "IX_refresh_tokens_TokenHash" ON auth.refresh_tokens ("TokenHash");

CREATE INDEX "IX_refresh_tokens_UserId" ON auth.refresh_tokens ("UserId");

CREATE INDEX "IX_role_claims_RoleId" ON auth.role_claims ("RoleId");

CREATE UNIQUE INDEX "RoleNameIndex" ON auth.roles ("NormalizedName");

CREATE INDEX "IX_user_claims_UserId" ON auth.user_claims ("UserId");

CREATE INDEX "IX_user_logins_UserId" ON auth.user_logins ("UserId");

CREATE INDEX "IX_user_roles_RoleId" ON auth.user_roles ("RoleId");

CREATE UNIQUE INDEX "IX_user_tokens_TokenHash" ON auth.user_tokens ("TokenHash");

CREATE INDEX "IX_user_tokens_UserId_Type" ON auth.user_tokens ("UserId", "Type");

CREATE INDEX "EmailIndex" ON auth.users ("NormalizedEmail");

CREATE INDEX "IX_users_DeletedAt" ON auth.users ("DeletedAt");

CREATE INDEX "IX_users_IsActive" ON auth.users ("IsActive");

CREATE UNIQUE INDEX "UserNameIndex" ON auth.users ("NormalizedUserName");

INSERT INTO "__EFMigrationsHistory" ("MigrationId", "ProductVersion")
VALUES ('20260717153236_InitialCreate', '8.0.0');

COMMIT;

